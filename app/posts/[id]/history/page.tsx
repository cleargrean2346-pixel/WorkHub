import Link from 'next/link';
import { notFound, redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
export default async function PostHistoryPage({params}:{params:Promise<{id:string}>}) { const {id}=await params; const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user)redirect('/login'); const {data:post}=await supabase.from('posts').select('id,title,author_id').eq('id',id).maybeSingle(); if(!post||post.author_id!==user.id)notFound(); const {data:revisions}=await supabase.from('post_revisions').select('id,title,excerpt,created_at').eq('post_id',id).order('created_at',{ascending:false}); return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href={`/posts/${id}`}><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Revision history</strong><small>{post.title}</small></div><Link className="back-link" href={`/posts/${id}/edit`}>Edit</Link></header><section className="workspace-content"><section className="workspace-panel"><div className="live-tasks">{(revisions??[]).map((revision)=><article className="live-task" key={revision.id}><div><b>{revision.title}</b><small>{revision.excerpt || 'No summary'} · {new Date(revision.created_at).toLocaleString('ko-KR')}</small></div></article>)}</div></section></section></main>; }
