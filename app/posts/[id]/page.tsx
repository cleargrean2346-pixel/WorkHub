import Link from 'next/link';
import { notFound } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { addComment, deletePost, toggleBookmark, toggleLike } from '../actions';
export default async function PostDetailPage({ params }: { params: Promise<{ id: string }> }) { const {id}=await params; const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); const {data:post}=await supabase.from('posts').select('id,title,body,status,created_at,author_id,view_count,cover_image_url').eq('id',id).maybeSingle(); if(!post || (!user && post.status!=='published')) notFound(); await supabase.rpc('increment_post_views',{target_post_id:post.id}); const {data:likes}=user?await supabase.from('likes').select('user_id').eq('post_id',post.id):{data:[]}; const {data:bookmark}=user?await supabase.from('bookmarks').select('post_id').eq('post_id',post.id).eq('user_id',user.id).maybeSingle():{data:null}; return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/posts"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Knowledge</strong><small>{post.status}</small></div><Link href="/posts" className="back-link">All posts</Link></header><section className="workspace-content"><article className="workspace-panel"><h1>{post.title}</h1><p>{new Date(post.created_at).toLocaleDateString('ko-KR')}</p>{post.cover_image_url && <img src={post.cover_image_url} alt="" style={{maxWidth:'100%',height:'auto'}}/>}<div style={{whiteSpace:'pre-wrap',lineHeight:1.7}}>{post.body || 'No content yet.'}</div>{user ? <div className="hero-actions"><form action={toggleLike}><input type="hidden" name="postId" value={post.id}/><button className="secondary">{likes?.some((item)=>item.user_id===user.id)?'Unlike':'Like'} · {likes?.length??0}</button></form><form action={toggleBookmark}><input type="hidden" name="postId" value={post.id}/><button className="secondary">{bookmark?'Bookmarked':'Bookmark'}</button></form>{user.id===post.author_id && <><Link className="secondary" href={`/posts/${post.id}/edit`}>Edit</Link><form action={deletePost}><input type="hidden" name="id" value={post.id}/><button className="secondary">Delete</button></form></>}</div> : <Link className="primary" href="/login">Log in to comment or like</Link>}</article>{user && <section className="workspace-panel"><h2>Comments</h2><form className="task-form" action={addComment}><input type="hidden" name="postId" value={post.id}/><textarea name="body" required maxLength={5000} placeholder="Add a comment"/><button className="primary">Comment</button></form></section>}</section></main>; }
