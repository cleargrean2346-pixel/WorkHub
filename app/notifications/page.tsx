import Link from 'next/link';import{redirect}from'next/navigation';import{createClient}from'@/lib/supabase/server';import{markAllNotificationsRead,markNotificationRead}from'./actions';
type N={id:string;title:string;body:string;link:string|null;kind:string;read_at:string|null;created_at:string};
export default async function NotificationsPage({searchParams}:{searchParams:Promise<{filter?:string;kind?:string}>}){const{filter='',kind=''}=await searchParams;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');let query=supabase.from('notifications').select('id,title,body,link,kind,read_at,created_at').eq('user_id',user.id);if(filter==='unread')query=query.is('read_at',null);if(kind)query=query.eq('kind',kind);const{data:rows}=await query.order('created_at',{ascending:false}).limit(100);const all=(rows??[])as N[];const unread=all.filter(n=>!n.read_at).length;const kinds=[...new Set(all.map(n=>n.kind))];return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Notifications</strong><small>{unread} unread</small></div><Link className="back-link" href="/workspace">Workspace</Link></header><section className="workspace-content"><div className="workspace-intro"><h1>Stay up to date</h1><p><Link href="/settings/notifications">Notification preferences</Link> · <Link href="/me/activity">My activity</Link></p><form action={markAllNotificationsRead}><button className="primary">Mark all as read</button></form></div><section className="workspace-panel"><form className="task-form"><select name="filter" defaultValue={filter}><option value="">All</option><option value="unread">Unread only</option></select><select name="kind" defaultValue={kind}><option value="">All types</option>{kinds.map(k=><option key={k} value={k}>{k}</option>)}</select><button className="secondary">Filter</button></form><div className="live-tasks">{all.map(n=><div className={`live-task ${n.read_at?'done':''}`} key={n.id}><div><b>{n.title}</b><small>{n.kind} · {n.body} · {new Date(n.created_at).toLocaleString('ko-KR')}</small></div>{n.link&&<Link className="secondary" href={n.link}>Open</Link>}{!n.read_at&&<form action={markNotificationRead}><input type="hidden" name="id" value={n.id}/><button className="secondary">Read</button></form>}</div>)}</div></section></section></main>;}
