import type { MetadataRoute } from 'next'; const base=process.env.NEXT_PUBLIC_APP_URL||'https://work-hub-ashen.vercel.app'; export default function sitemap():MetadataRoute.Sitemap{return ['/','/login','/notices','/terms','/privacy','/data-policy','/cookies'].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path==='/'?'weekly':'monthly',priority:path==='/'?1:0.5}));}
