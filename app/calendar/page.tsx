import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
export default async function CalendarPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); const { data: membership } = await supabase.from('organization_members').select('organization_id').eq('user_id', user.id).eq('status','approved').limit(1).maybeSingle(); if (!membership) redirect('/workspace'); const { data: tasks } = await supabase.from('work_tasks').select('id,title,start_at,due_at,status,priority').eq('organization_id',membership.organization_id).is('archived_at',null).order('due_at'); const dated = (tasks ?? []).filter((task) => task.due_at || task.start_at); return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Schedule</strong><small>Upcoming task dates</small></div><Link className="back-link" href="/tasks">Tasks</Link></header><section className="workspace-content"><section className="workspace-panel"><h1>Schedule</h1>{dated.length ? dated.map((task) => <Link className="live-task" href={`/tasks/${task.id}`} key={task.id}><div><b>{task.title}</b><small>{task.start_at || 'No start'} → {task.due_at || 'No due date'} · {task.status} · {task.priority}</small></div></Link>) : <p>No scheduled tasks yet.</p>}</section></section></main>; }
