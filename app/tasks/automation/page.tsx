import Link from 'next/link'; import { redirect } from 'next/navigation'; import { createClient } from '@/lib/supabase/server';
export default async function TaskAutomationPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const{data:m}=await supabase.from('organization_members').select('organization_id').eq('user_id',user.id).eq('status','approved').limit(1).maybeSingle();if(!m)redirect('/workspace');const{data:tasks}=await supabase.from('work_tasks').select('id,title,recurrence,reminder_at,due_at,status').eq('organization_id',m.organization_id).is('archived_at',null).not('recurrence','is',null).order('due_at');return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Task automation</strong><small>{tasks?.length??0} recurring tasks</small></div><Link className="back-link" href="/tasks">Tasks</Link></header><section className="workspace-content"><section className="workspace-panel"><h1>Recurring tasks</h1><p>When a recurring task is completed, the next task is created automatically.</p>{tasks?.length?<div className="live-tasks">{tasks.map(t=><Link className="live-task" href={`/tasks/${t.id}`} key={t.id}><div><b>{t.title}</b><small>{t.recurrence} · due {t.due_at||'not set'} · reminder {t.reminder_at?new Date(t.reminder_at).toLocaleString('ko-KR'):'not set'} · {t.status}</small></div></Link>)}</div>:<div className="empty-state">No recurring tasks yet. Set a repeat option in task details.</div>}</section></section></main>;}
