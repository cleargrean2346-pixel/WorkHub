import Link from 'next/link';
import { notFound, redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { setTaskReminder } from '../../actions';

export default async function TaskReminderPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); const { data: task } = await supabase.from('work_tasks').select('id,title,reminder_at').eq('id', id).maybeSingle(); if (!task) notFound(); return <main className="onboarding"><section className="onboarding-card"><Link className="brand" href={`/tasks/${id}`}><span className="brand-mark">W</span><span>workhub</span></Link><p className="eyebrow"><span /> 알림</p><h1>업무 알림 설정</h1><p>{task.title}</p><form action={setTaskReminder}><input type="hidden" name="id" value={id} /><label>알림 시각<input name="reminderAt" type="datetime-local" defaultValue={task.reminder_at ? new Date(task.reminder_at).toISOString().slice(0, 16) : ''} /></label><button className="primary">알림 저장</button></form><Link className="back-link" href={`/tasks/${id}`}>업무 상세로</Link></section></main>; }
