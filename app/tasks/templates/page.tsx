import Link from'next/link';import{redirect}from'next/navigation';import{createClient}from'@/lib/supabase/server';import{createFromTemplate,createTemplate}from'./actions';
export default async function TemplatesPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const{data:m}=await supabase.from('organization_members').select('organization_id').eq('user_id',user.id).eq('status','approved').limit(1).maybeSingle();if(!m)redirect('/workspace');const{data:templates}=await supabase.from('task_templates').select('id,name,description,priority,labels,recurrence').eq('organization_id',m.organization_id).order('created_at',{ascending:false});return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Task templates</strong><small>Reuse recurring work setup</small></div><Link className="back-link" href="/tasks">Tasks</Link></header><section className="workspace-content"><section className="workspace-panel"><form className="task-form" action={createTemplate}><input name="name" placeholder="Template name" required/><textarea name="description" placeholder="Description"/><select name="priority" defaultValue="medium"><option value="high">High</option><option value="medium">Medium</option><option value="low">Low</option></select><input name="labels" placeholder="Labels, comma separated"/><select name="recurrence"><option value="">No repeat</option><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option></select><button className="primary">Save template</button></form></section><section className="workspace-panel">{(templates??[]).map(t=><div className="live-task" key={t.id}><div><b>{t.name}</b><small>{t.priority} · {t.recurrence||'one time'} · {t.labels?.join(', ')}</small></div><form action={createFromTemplate}><input type="hidden" name="id" value={t.id}/><button className="secondary">Create task</button></form></div>)}</section></section></main>;}
