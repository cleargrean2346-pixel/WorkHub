import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { removeAiSettings, saveAiSettings } from './actions';
export default async function AiSettingsPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); const { data: settings } = await supabase.from('user_ai_settings').select('provider,model,base_url,updated_at,last_tested_at').eq('user_id', user.id).maybeSingle(); return <main className="onboarding"><section className="onboarding-card"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><p className="eyebrow"><span /> PRIVATE AI SETTINGS</p><h1>내 AI API 키 연결</h1><p>API 키는 서버에서 암호화되며 저장 후 다시 표시되지 않습니다. 새 키를 입력하면 기존 키가 교체됩니다.</p>{settings && <p>저장된 제공자: <b>{settings.provider}</b>{settings.model ? ` · ${settings.model}` : ''}<br />최근 연결 테스트: {settings.last_tested_at ? new Date(settings.last_tested_at).toLocaleString('ko-KR') : '아직 없음'}</p>}<form action={saveAiSettings}><label htmlFor="provider">AI 제공자</label><select id="provider" name="provider" defaultValue={settings?.provider || 'openai'}><option value="openai">OpenAI</option><option value="anthropic">Anthropic</option><option value="gemini">Google Gemini</option><option value="openrouter">OpenRouter</option><option value="deepseek">DeepSeek</option><option value="groq">Groq</option><option value="compatible">OpenAI 호환 API</option></select><label htmlFor="apiKey">API 키</label><input id="apiKey" name="apiKey" type="password" placeholder={settings ? '새 키를 입력하면 기존 키를 교체합니다' : 'API 키를 붙여 넣으세요'} required autoComplete="off"/><label htmlFor="model">기본 모델 (선택)</label><input id="model" name="model" defaultValue={settings?.model || ''} placeholder="예: gpt-5"/><label htmlFor="baseUrl">사용자 지정 Base URL (선택)</label><input id="baseUrl" name="baseUrl" defaultValue={settings?.base_url || ''} placeholder="https://api.example.com/v1"/><button className="primary">암호화하여 저장</button></form><div className="hero-actions"><Link className="secondary" href="/ai">연결 테스트·AI 도구</Link>{settings && <form action={removeAiSettings}><button className="text-button">저장된 키 삭제</button></form>}</div><Link className="back-link" href="/workspace">내 공간</Link></section></main>; }
