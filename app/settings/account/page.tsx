import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { deactivateMyAccount, requestDataDeletion } from './actions';
export default async function AccountSettingsPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>계정 관리</strong><small>{user.email}</small></div><Link className="back-link" href="/me">마이페이지</Link></header><section className="workspace-content"><div className="workspace-intro"><p className="eyebrow"><span /> ACCOUNT</p><h1>내 계정과 데이터</h1><p>내 데이터 사본을 내려받거나, 서비스 사용을 중단할 수 있습니다.</p></div><section className="workspace-panel"><h2>내 데이터 내보내기</h2><p>프로필, 게시글, 댓글, 반응, 업무, 알림을 JSON 형식으로 내려받습니다.</p><a className="primary" href="/api/me/export">내 데이터 내려받기</a></section><section className="workspace-panel"><h2>삭제·익명화 요청</h2><p>법적 보관 의무가 없는 데이터는 관리자 검토 후 삭제 또는 익명화할 수 있습니다.</p><form className="task-form" action={requestDataDeletion}><select name="requestType"><option value="delete">계정 데이터 삭제 요청</option><option value="anonymize">계정 데이터 익명화 요청</option></select><textarea name="reason" maxLength={1000} placeholder="요청 사유 (선택)"/><button className="secondary">관리자에게 요청 보내기</button></form></section><section className="workspace-panel"><h2>계정 비활성화</h2><p>비활성화하면 모든 워크스페이스 접근이 중지되고 로그아웃됩니다.</p><form className="task-form" action={deactivateMyAccount}><label>확인 문구<input name="confirmation" placeholder="비활성화" required /></label><button className="secondary">내 계정 비활성화</button></form></section></section></main>; }
