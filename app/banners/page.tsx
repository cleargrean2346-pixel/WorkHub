import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';

type Banner = { id: string; title: string; message: string; link_url: string | null; placement: string };
const placement = (value: string) => ({ top: '상단', sidebar: '사이드바', footer: '하단' }[value] ?? value);
export default async function BannersPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return <main className="onboarding"><section className="onboarding-card"><h1>안내를 보려면 로그인하세요</h1><Link className="primary" href="/login">로그인</Link></section></main>; const { data: member } = await supabase.from('organization_members').select('organization_id').eq('user_id', user.id).eq('status', 'approved').limit(1).maybeSingle(); if (!member) return <main className="onboarding"><section className="onboarding-card"><h1>워크스페이스 승인이 필요합니다</h1><Link className="primary" href="/workspace">내 공간</Link></section></main>; const { data: rows } = await supabase.from('banners').select('id,title,message,link_url,placement').eq('organization_id', member.organization_id).eq('status', 'published').order('created_at', { ascending: false }); const banners = (rows ?? []) as Banner[]; return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>워크스페이스 안내</strong><small>중요한 업데이트</small></div><Link className="back-link" href="/workspace">내 공간</Link></header><section className="workspace-content"><div className="workspace-intro"><p className="eyebrow"><span /> 안내</p><h1>워크스페이스 업데이트</h1><p>조직에 게시된 중요한 안내를 확인하세요.</p></div><section className="workspace-panel"><div className="workspace-panel-title"><h2>게시된 배너</h2><span>{banners.length}</span></div>{banners.length ? <div className="live-tasks">{banners.map((banner) => <article className="live-task" key={banner.id}><div><b>{banner.title}</b><small>{placement(banner.placement)} · {banner.message}</small></div>{banner.link_url && <a className="secondary" href={banner.link_url} target="_blank" rel="noreferrer">열기</a>}</article>)}</div> : <div className="empty-state">게시된 안내 배너가 없습니다.</div>}</section></section></main>; }
