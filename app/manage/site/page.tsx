import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { updateSiteSettings } from './actions';

export default async function SiteSettingsPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); const { data: membership } = await supabase.from('organization_members').select('organization_id,role').eq('user_id', user.id).eq('status', 'approved').limit(1).maybeSingle(); if (!membership || !['organization_admin', 'system_admin'].includes(membership.role)) return <main className="onboarding"><section className="onboarding-card"><h1>관리자 권한이 필요합니다</h1><Link className="primary" href="/workspace">내 공간</Link></section></main>; const [{ data: settings }, { data: categories }] = await Promise.all([supabase.from('site_settings').select('site_title,site_description,maintenance_enabled,home_category_ids').eq('id', true).maybeSingle(), supabase.from('categories').select('id,name').eq('organization_id', membership.organization_id).order('name')]); const selected = settings?.home_category_ids ?? []; return <main className="onboarding"><section className="onboarding-card"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><p className="eyebrow"><span /> 관리자</p><h1>사이트 설정</h1><p>사이트 정보와 홈 화면 캘린더 아래에 표시할 카테고리를 설정하세요.</p><form action={updateSiteSettings}><label>사이트 제목<input name="siteTitle" defaultValue={settings?.site_title || 'WorkHub'} required maxLength={100} /></label><label>사이트 설명<textarea name="siteDescription" defaultValue={settings?.site_description || ''} required maxLength={300} rows={4} /></label><fieldset><legend>홈 카테고리</legend><p>홈 화면의 캘린더 아래에 표시할 카테고리를 선택하세요.</p>{categories?.length ? categories.map((category) => <label key={category.id}><input type="checkbox" name="homeCategoryIds" value={category.id} defaultChecked={selected.includes(category.id)} /> {category.name}</label>) : <p>생성된 카테고리가 없습니다. 먼저 카테고리·태그 관리에서 카테고리를 추가하세요.</p>}</fieldset><label><input type="checkbox" name="maintenanceEnabled" defaultChecked={settings?.maintenance_enabled || false} /> 점검 모드</label><button className="primary">사이트 설정 저장</button></form><Link className="back-link" href="/manage/dashboard">관리자로</Link></section></main>; }
