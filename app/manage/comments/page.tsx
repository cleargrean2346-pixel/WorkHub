import Link from 'next/link';
import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { moderateComment } from './actions';
export default async function ManageCommentsPage() { const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user)redirect('/login'); const {data:member}=await supabase.from('organization_members').select('organization_id,role').eq('user_id',user.id).eq('status','approved').limit(1).maybeSingle(); if(!member||!['organization_admin','system_admin'].includes(member.role))return <main className="onboarding"><section className="onboarding-card"><h1>Administrator access required</h1><Link className="primary" href="/workspace">Workspace</Link></section></main>; const {data:comments}=await supabase.from('comments').select('id,body,created_at,post_id,posts!inner(title,organization_id)').eq('posts.organization_id',member.organization_id).order('created_at',{ascending:false}).limit(100); return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Comment moderation</strong><small>Recent organization comments</small></div><Link className="back-link" href="/workspace">Workspace</Link></header><section className="workspace-content"><section className="workspace-panel"><div className="live-tasks">{(comments??[]).map((comment:any)=><div className="live-task" key={comment.id}><div><b>{comment.posts?.title || 'Post'}</b><small>{comment.body} · {new Date(comment.created_at).toLocaleString('ko-KR')}</small></div><form action={moderateComment}><input type="hidden" name="id" value={comment.id}/><button className="secondary">Delete</button></form></div>)}</div></section></section></main>; }
