import Link from 'next/link'; import {redirect} from 'next/navigation'; import {createClient} from '@/lib/supabase/server'; import {moderateComment} from './actions';
export default async function ManageCommentsPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const{data:m}=await supabase.from('organization_members').select('organization_id,role').eq('user_id',user.id).eq('status','approved').limit(1).maybeSingle();if(!m||!['organization_admin','system_admin'].includes(m.role))return <main className="onboarding"><section className="onboarding-card"><h1>Administrator access required</h1><Link className="primary" href="/workspace">Workspace</Link></section></main>;const{data:comments}=await supabase.from('comments').select('id,body,created_at,post_id,posts!inner(title,organization_id)').eq('posts.organization_id',m.organization_id).order('created_at',{ascending:false}).limit(100);return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Comment moderation</strong><small>{comments?.length??0} recent comments</small></div><Link className="back-link" href="/manage/dashboard">Admin</Link></header><section className="workspace-content"><div className="workspace-intro"><p className="eyebrow"><span/> ADMIN</p><h1>Moderate organization comments</h1><p>Review recent comments before permanently removing inappropriate content.</p></div><section className="workspace-panel"><div className="workspace-panel-title"><h2>Comments</h2><span>{comments?.length??0}</span></div>{comments?.length?<div className="live-tasks">{comments.map((c:any)=><div className="live-task" key={c.id}><div><b>{c.posts?.title||'Post'}</b><small>{c.body} · {new Date(c.created_at).toLocaleString('ko-KR')}</small></div><form action={moderateComment}><input type="hidden" name="id" value={c.id}/><button className="secondary">Delete</button></form></div>)}</div>:<div className="empty-state">No comments need moderation.</div>}</section></section></main>}
