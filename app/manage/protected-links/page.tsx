import Link from 'next/link'; import {redirect} from 'next/navigation'; import {createClient} from '@/lib/supabase/server'; import {createProtectedLink,setProtectedLinkEnabled} from './actions';
type ProtectedLink={id:string;post_id:string;original_url:string;protected_token:string;enabled:boolean;posts:{title:string}|{title:string}[]|null};
export default async function ProtectedLinksPage(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const{data:m}=await supabase.from('organization_members').select('organization_id,role').eq('user_id',user.id).eq('status','approved').limit(1).maybeSingle();if(!m||!['organization_admin','system_admin'].includes(m.role))return <main className="onboarding"><section className="onboarding-card"><h1>Administrator access required</h1><Link className="primary" href="/workspace">Back to workspace</Link></section></main>;const[{data:posts},{data:rows}]=await Promise.all([supabase.from('posts').select('id,title').eq('organization_id',m.organization_id).order('created_at',{ascending:false}),supabase.from('protected_urls').select('id,post_id,original_url,protected_token,enabled,posts(title)').order('created_at',{ascending:false})]);const links=(rows??[])as unknown as ProtectedLink[];const appUrl=process.env.NEXT_PUBLIC_APP_URL||'https://work-hub-ashen.vercel.app';return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>Protected links</strong><small>{links.length} secure links</small></div><Link className="back-link" href="/manage/dashboard">Admin</Link></header><section className="workspace-content"><div className="workspace-intro"><p className="eyebrow"><span/> ADMIN</p><h1>Protect external URLs</h1><p>Destination URLs are resolved by the server after membership verification.</p></div><section className="workspace-panel"><form className="task-form" action={createProtectedLink}><select name="postId" required><option value="">Choose a post</option>{(posts??[]).map(p=><option key={p.id} value={p.id}>{p.title}</option>)}</select><input name="originalUrl" type="url" placeholder="https://example.com/private-resource" required/><button className="primary">Create protected link</button></form></section><section className="workspace-panel"><div className="workspace-panel-title"><h2>Links</h2><span>{links.length}</span></div>{links.length?<div className="live-tasks">{links.map(link=>{const post=Array.isArray(link.posts)?link.posts[0]:link.posts;return <div className="live-task" key={link.id}><div><b>{post?.title||'Post'}</b><small>{appUrl}/go/{link.protected_token} · {link.enabled?'enabled':'disabled'}</small></div><form action={setProtectedLinkEnabled}><input type="hidden" name="id" value={link.id}/><input type="hidden" name="enabled" value={String(!link.enabled)}/><button className="secondary">{link.enabled?'Disable':'Enable'}</button></form></div>})}</div>:<div className="empty-state">No protected links have been created yet.</div>}</section></section></main>}
