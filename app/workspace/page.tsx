import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
import { createOrganization, createTask, setTaskStatus } from './actions';
import './workspace.css';
type Task = { id: string; title: string; priority: string; status: string; created_at: string };
export default async function WorkspacePage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return null; const { data: memberships } = await supabase.from('organization_members').select('organization_id, organizations(id, name)').eq('user_id', user.id); const organization = memberships?.[0]?.organizations as unknown as { id: string; name: string } | null; if (!organization) return <main className="onboarding"><section className="onboarding-card"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><p className="eyebrow"><span /> WELCOME</p><h1>첫 워크스페이스를 만들어요</h1><p>조직의 업무, 문서, 구성원, AI 설정은 이 워크스페이스 안에서 안전하게 분리됩니다.</p><form action={createOrganization}><label htmlFor="name">조직 이름</label><input id="name" name="name" placeholder="예: Jupiter Labs" required maxLength={80} /><button className="primary">워크스페이스 만들기 →</button></form></section></main>; const { data: tasks } = await supabase.from('work_tasks').select('id, title, priority, status, created_at').eq('organization_id', organization.id).order('created_at', { ascending: false }).limit(30); const pending = (tasks as Task[] | null)?.filter(task => task.status !== 'done').length ?? 0; return <main className="workspace-page"><header className="workspace-header"><Link className="brand" href="/"><span className="brand-mark">W</span><span>workhub</span></Link><div><strong>{organization.name}</strong><small>내 워크스페이스</small></div><Link href="/" className="back-link">대시보드로 돌아가기</Link></header><section className="workspace-content"><div className="workspace-intro"><p className="eyebrow"><span /> YOUR WORKSPACE</p><h1>업무를 한곳에서 관리하세요</h1><p>미완료 작업 <b>{pending}</b>개가 있습니다.</p></div><div className="workspace-grid"><section className="workspace-panel"><h2>새 작업</h2><form className="task-form" action={createTask}><input type="hidden" name="organizationId" value={organization.id} /><label htmlFor="task-title">작업 이름</label><input id="task-title" name="title" placeholder="무엇을 해야 하나요?" required maxLength={200} /><label htmlFor="priority">우선순위</label><select id="priority" name="priority" defaultValue="medium"><option value="high">높음</option><option value="medium">보통</option><option value="low">낮음</option></select><button className="primary">작업 추가</button></form></section><section className="workspace-panel"><div className="workspace-panel-title"><h2>작업 목록</h2><span>{tasks?.length ?? 0}개</span></div>{tasks?.length ? <div className="live-tasks">{(tasks as Task[]).map(task => <form action={setTaskStatus} className={`live-task ${task.status === 'done' ? 'done' : ''}`} key={task.id}><input type="hidden" name="id" value={task.id} /><input type="hidden" name="status" value={task.status === 'done' ? 'todo' : 'done'} /><button aria-label={`${task.title} 완료 상태 변경`} className="task-toggle">{task.status === 'done' ? '✓' : ''}</button><div><b>{task.title}</b><small>{task.priority === 'high' ? '높음' : task.priority === 'low' ? '낮음' : '보통'} 우선순위</small></div></form>)}</div> : <div className="empty-state">아직 작업이 없습니다. 첫 작업을 만들어 보세요.</div>}</section></div></section></main>; }
