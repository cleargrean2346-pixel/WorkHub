'use server';
import { revalidatePath } from 'next/cache';
import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
const slugify = (value: string) => value.toLowerCase().trim().replace(/[^a-z0-9가-힣]+/g, '-').replace(/(^-|-$)/g, '');
export async function createOrganization(formData: FormData) { const name = String(formData.get('name') ?? '').trim(); if (!name) return; const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); const { error } = await supabase.from('organizations').insert({ name, slug: `${slugify(name) || 'workspace'}-${crypto.randomUUID().slice(0, 8)}`, created_by: user.id }); if (error) throw new Error('워크스페이스를 만들지 못했습니다.'); revalidatePath('/workspace'); }
export async function createTask(formData: FormData) { const title = String(formData.get('title') ?? '').trim(); const organizationId = String(formData.get('organizationId') ?? ''); const priority = String(formData.get('priority') ?? 'medium'); if (!title || !organizationId || !['low', 'medium', 'high'].includes(priority)) return; const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect('/login'); const { error } = await supabase.from('work_tasks').insert({ organization_id: organizationId, creator_id: user.id, assignee_id: user.id, title, priority }); if (error) throw new Error('작업을 만들지 못했습니다.'); revalidatePath('/workspace'); }
export async function setTaskStatus(formData: FormData) { const id = String(formData.get('id') ?? ''); const status = String(formData.get('status') ?? 'todo'); if (!id || !['todo', 'done'].includes(status)) return; const supabase = await createClient(); const { error } = await supabase.from('work_tasks').update({ status }).eq('id', id); if (error) throw new Error('작업 상태를 변경하지 못했습니다.'); revalidatePath('/workspace'); }
